{
    "name": "ETL Testing Environment",
    "image": "mcr.microsoft.com/vscode/devcontainers/python:3.9",
    "extensions": [
        "ms-python.python",
        "ms-azuretools.vscode-docker",
        "hashicorp.terraform"
    ],
    "postCreateCommand": "pip install -r requirements.txt && curl -fsSL https://apt.releases.hashicorp.com/gpg | sudo apt-key add - && sudo apt-add-repository 'deb [arch=amd64] https://apt.releases.hashicorp.com focal main' && sudo apt-get update && sudo apt-get install -y terraform"
}